name: docs-lint

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint docs (frontmatter, INV references, version consistency)
        run: python3 scripts/lint_docs.py
